# abbies-corpus-creator
A standalone HTML file that takes a raw AI chat thread as input, sends it to the API, and downloads a structured `.md` corpus file as output. No install. No backend. Opens in any browser.
